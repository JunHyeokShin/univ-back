spring:
  application:
    name: univ
  datasource:
    url: jdbc:mysql://localhost:3306/univ?characterEncoding=utf-8
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  secret: ThisIsAVeryLongSecretKeyForJWTSignatureMustBeAtLeast256BitsLongForHS256Algorithm
  expiration-ms: 3600000  # 한 시간
